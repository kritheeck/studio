import { AppShell } from "@/components/app-shell";
import { ReformatClient } from "./reformat-client";

export default function ReformatPage() {
  return (
    <AppShell>
      <ReformatClient />
    </AppShell>
  );
}
